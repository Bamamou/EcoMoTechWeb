# Environment configuration for subdomain deployment
# This file should be used as a reference for setting up environment variables

# Deployment target - tells the app how to configure paths and routing
VITE_DEPLOYMENT_TARGET=subdomain

# Base path for the application - root path for subdomain
VITE_APP_BASENAME=/

# API configuration (if needed for contact forms)
# These should be set in your hosting provider's environment or GitHub secrets
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# HR_EMAIL=ecomotech@outlook.com

# Production environment
NODE_ENV=production