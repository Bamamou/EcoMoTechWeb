name: Deploy EcoMoTech to Namecheap      - name: 🚀 Deploy to Namecheap via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          protocol: ftp
          local-dir: ./dist/
          server-dir: ${{ secrets.FTP_DIR }}/
          timeout: 60000
          log-level: verbose branches: [ "main" ]
  workflow_dispatch:  # Allow manual deployment

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚀 Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Run build
        run: npm run build

      - name: 🧪 Verify build output
        run: |
          echo "Build completed. Contents of dist folder:"
          ls -la dist/
          echo "Checking if index.html exists:"
          test -f dist/index.html && echo "✅ index.html found" || echo "❌ index.html missing"
          echo "Checking if .htaccess exists:"
          test -f dist/.htaccess && echo "✅ .htaccess found" || echo "❌ .htaccess missing"

      - name: � Debug FTP Connection
        run: |
          echo "🔍 Debugging FTP connection..."
          echo "Server: ${{ secrets.FTP_SERVER }}"
          echo "Username: ${{ secrets.FTP_USERNAME }}"
          echo "Directory: ${{ secrets.FTP_DIR }}"
          echo "Attempting to ping server..."
          # Try to resolve the hostname
          nslookup ${{ secrets.FTP_SERVER }} || echo "⚠️ Could not resolve hostname"

      - name: �🚀 Deploy to Namecheap via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          protocol: ftp
          local-dir: ./dist/
          server-dir: ${{ secrets.FTP_DIR }}/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            .github/
            README.md
          dangerous-clean-slate: false
          dry-run: false

      - name: 🎉 Deployment Success
        if: success()
        run: |
          echo "✅ Deployment completed successfully!"
          echo "🌐 Your site should be available at: https://ecomotech.online"
          
      - name: 💥 Deployment Failed  
        if: failure()
        run: |
          echo "❌ Deployment failed. Please check the logs above."
